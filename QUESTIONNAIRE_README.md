# سیستم پرسشنامه پزشکی

## 📋 توضیحات

فایل `questionnaire-multi.html` یک سیستم پرسشنامه مدرن و حرفه‌ای است که امکان انتخاب بین دو پرسشنامه مختلف را فراهم می‌کند.

## ✨ ویژگی‌های اصلی

### 1. **سیستم انتخاب پرسشنامه**
- دو تب برای انتخاب پرسشنامه اول و دوم
- جابه‌جایی نرم (Animation) بین پرسشنامه‌ها
- هر پرسشنامه فرم مستقل خود را دارد

### 2. **پرسشنامه اول**
شامل:
- کد شناسایی
- سن
- دو سوال ساده

### 3. **پرسشنامه دوم** (پیشرفته‌تر)
شامل:
- کد شناسایی و سن
- **تاریخ** (اضافی)
- قسمت ارزیابی:
  - میزان رضایت
  - نظر کتبی
  - سطح کمک دریافتی
- بخش اضافی:
  - تعداد جلسات
  - توصیه برای دوره دوباره

## 🚀 استفاده

### در مرورگر

```bash
# فایل را در مرورگر باز کنید
file:///path/to/questionnaire-multi.html
```

یا از GitHub Pages استفاده کنید:
```
https://hadiebrahimiseraji.github.io/medicalpromaxproject/questionnaire-multi.html
```

### میانبر‌های صفحه‌کلید

| میانبر | عملکرد |
|-------|--------|
| `Ctrl + Enter` | ذخیره و ثبت فرم |
| `Ctrl + R` | پاک کردن فرم |

## 💾 ذخیره داده‌ها

تمام داده‌های فرم در **localStorage** ذخیره می‌شوند:

```javascript
// داده‌های ذخیره شده
localStorage.questionnaire_records = JSON.stringify([
  {
    timestamp: "7/2/1404، 11:47 صبح",
    questionnaire: "q1",
    data: {
      code: "1234",
      age: "25",
      q1_s1: "1",
      q1_s2: "2"
    }
  }
])
```

### بازیابی داده‌ها از Console

```javascript
// در DevTools کنسول:
var records = JSON.parse(localStorage.getItem('questionnaire_records'));
console.table(records);
```

## 🎨 طراحی و رنگ‌بندی

- **رنگ اصلی**: آبی-بنفش (`#667eea` - `#764ba2`)
- **رنگ موفقیت**: سبز (`#4caf50`)
- **Direction**: راست به چپ (RTL) برای فارسی
- **Font**: IRANSans / Segoe UI

## 📱 ریسپانسیو

فرم برای تمام دستگاه‌ها بهینه‌شده است:
- دسکتاپ (عرض > 768px)
- تبلت (عرض 480px - 768px)
- موبایل (عرض < 480px)

## 🔄 فرآیند استفاده

1. **انتخاب پرسشنامه**
   - کلیک روی تب "پرسشنامه اول" یا "پرسشنامه دوم"
   - فرم خودکار به بالا اسکرول می‌شود

2. **تکمیل فرم**
   - فیلدهای ضروری (*) را پر کنید
   - روی دکمه‌های گزینه کلیک کنید

3. **ذخیره**
   - دکمه "💾 ذخیره و ثبت" را بزنید
   - یا `Ctrl + Enter` را فشار دهید

4. **تأیید موفقیت**
   - پیام سبز "✓ ذخیره شد" نمایش می‌یابد
   - فرم خودکار پاک می‌شود

## 🛠️ راهنمای توسعه

### اضافه کردن پرسشنامه جدید

```html
<!-- تب جدید -->
<button type="button" class="tab-btn" data-questionnaire="q3">
پرسشنامه سوم</button>

<!-- فرم جدید -->
<form id="form3" class="form-container" data-questionnaire="q3">
  <!-- محتوای فرم -->
</form>
```

### اضافه کردن سوال جدید

```html
<div class="question-group">
  <label class="question-label">سوال جدید</label>
  <div class="options-grid">
    <button type="button" class="option-btn q1-option" 
            data-field="q1_new" data-value="1">گزینه 1</button>
    <button type="button" class="option-btn q1-option" 
            data-field="q1_new" data-value="2">گزینه 2</button>
  </div>
</div>
```

## 📊 صادور داده‌ها

### برای دریافت تمام داده‌ها (Console):

```javascript
var records = JSON.parse(localStorage.getItem('questionnaire_records'));
var csv = 'timestamp,questionnaire,code,age\n';
records.forEach(r => {
  csv += `${r.timestamp},${r.questionnaire},${r.data.code},${r.data.age}\n`;
});
copy(csv);
```

## ⚠️ نکات مهم

1. **localStorage محدود است**: تا ~5-10MB برای هر سایت
2. **خصوصی بودن**: داده‌ها در مرورگر ذخیره می‌شوند، سرور ندارد
3. **تهی کردن**: `localStorage.clear()` تمام داده‌ها را حذف می‌کند
4. **Backup**: به طور منظم داده‌ها را صادور کنید

## 🚀 نسخه‌های آینده

- [ ] اتصال به Backend (API)
- [ ] صادور Excel/CSV
- [ ] نمودار و آمار
- [ ] کاربران و احراز هویت
- [ ] توسعه پرسشنامه‌های بیشتر

## 📞 تماس

برای سوال یا پیشنهاد:
- ایمیل: [ebrahimiserajihadi@gmail.com](mailto:ebrahimiserajihadi@gmail.com)
- GitHub: [@Hadiebrahimiseraji](https://github.com/Hadiebrahimiseraji)

---

**آخرین بروزرسانی**: 7 فوریه 2026
